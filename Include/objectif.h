#ifndef OBJECTIF_H_
#define OBJECTIF_H_
#include <stdlib.h>
#include <unistd.h>
#include <stdio.h>
unsigned int stu_strlen(const char *str);
char *rev(char *buffer);
int *reverses(char *buffer);
char *stu_strcat(char *a, const char *b);
char *strdup(const char *s);
char *stu_strcpy(char *dest, const char *src);
#endif /*OBJECTIF_H_*/
